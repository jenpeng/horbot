export const DEFAULTS = {
  PAGE_SIZE: 20,
  LARGE_PAGE_SIZE: 50,
  
  LOG_LINES_DEFAULT: 100,
  LOG_LINES_OPTIONS: [50, 100, 200, 500],
  
  REFRESH_INTERVAL_MS: 10000,
  FAST_REFRESH_INTERVAL_MS: 5000,
  SLOW_REFRESH_INTERVAL_MS: 30000,
  
  TOAST_DURATION_MS: 3000,
  
  DEBOUNCE_DELAY_MS: 300,
  THROTTLE_DELAY_MS: 100,
  
  MAX_RETRIES: 3,
  RETRY_DELAY_MS: 1000,
  
  SIDEBAR_WIDTH: 240,
  HEADER_HEIGHT: 56,
} as const;
